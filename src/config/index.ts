const config = {
    url: {
        api: "https://steroidapp.ddns.net/api",
        fallback: null,
        app: "http://localhost:7665",
        service: "http://localhost:7666",
    },
};

export default config;